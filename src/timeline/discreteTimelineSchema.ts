import type { ClassDoc } from '../api/visualBuilder';

export const JUMP_TO_SCHEMA: ClassDoc = {
  className: 'jump_to',
  constructorParams: 't: int',
  docstring:
    'Required function for discrete animation mode. Called for each integer time step t in [0, T].',
  properties: [],
  methods: [],
};

