- This is not an algorithm walkthrough, but rather use an algorithm to learn how to use the program. It should appear in the getting started area or right after it, before features and algorithm walkthrough. In particular, we should change the title to indicate that we use "selection sort" to learn how to use the program, and selection sort by itself is not the main focus.

- First paragraph: "Selection sort finds the minimum element in the unsorted region and swaps it into place — one element per pass. As a first basic example, we will use it to learn the interesting feature that AlgoPlay has." or something like that

- TODO: add to the embedded version an option to set the viewing window. Then in this example set the array itself in the center (will it work with different browsers \ phones?). Also maybe choose initial step.

# First step

- At the end of the first description, write something like: "notice that the viz blocks define an `Array`, a `Rect` a couple of `Arrow`s and `Label`s, which we can see in the visualization itself. More details about these shapes and more can be find in (link)." This last line should be here instead of right before Step1.

- in the code in step one, highlight the viz blocks similarly to the actual program.

- I like the comment after the code. Maybe even write "much simpler really soon!"

# Step2

- TODO: add another image of the program, but where the panel is visible

- code: here too highlight the viz block

- I like the comments below the code. Maybe add a bit more color? suggestions?

# Step3

- Before the code, in the explanation of V() say "for example the min_index arrow should be binded to the min_index value" 

- "Pre-initialize variables": just mention that V() can have defaults, and link to feature-4-tracing.

- "Custom Panel subclass": Not "it accepts V('...')", but rather "it can accept V('... ')".